name: Java pipeline

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: latest
        env:
          MYSQL_ROOT_PASSWORD: 23fevrier
          MYSQL_DATABASE: banque
          MYSQL_USER: root
          MYSQL_PASSWORD: 23fevrier
        ports:
          - 3306/tcp


    steps:
      - name: Recuperation du code
        uses: actions/checkout@v2

      - name: Setup JDK
        uses: actions/setup-java@v1
        with:
          distribution: 'oracle'
          java-version: '17'
          cache: maven

      - name: Installation des dependances
        run: mvn package

      - name: Build du projet
        run: mvn clean install -X

      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: Java artifact
          path: target/banque-0.0.1-SNAPSHOT.jar

